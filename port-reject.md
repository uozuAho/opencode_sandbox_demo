Port scripts/reject.sh to a typer command in ./src/soc_cli/commands/
_common.sh functionality has already been ported to ./src/soc_cli/common.py and ./src/sbx

Use subprocess to run git commands. rm can be substituted with python library functions.
